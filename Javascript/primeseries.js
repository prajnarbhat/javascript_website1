const prompt = require("prompt-sync")();
const lower=parseInt(prompt("Enter a lower number"))
const higher=parseInt(prompt("enter a higher number"));

for(let i=lower;i<=higher;i++){
    let flag=0;
    for(let j=2;j<i/2;j++){
        
    }
}